### install
- cd `pwd`/app
- sh install.sh
- add  ~/.ssh/id_rsa.pub into vps's authorized_keys file
  repalce your vps's user and password in bin/start_socks.sh 

## usage
- sh start.sh
- set auto proxy:http://127.0.0.1:12820/proxy.pac
  let's go world!!!

## desc
port:
12800 -- 12809, ssh sock;
12812 -- sock proxy;
12820 -- pac web server;


## qa
- when something go wrong, two step can help you:
one: restart nginx and sock5 (sh start.sh)
two: enter chrome://net-internals/#proxy in chrome , click 'Proxy/ Re-apply settings'.
